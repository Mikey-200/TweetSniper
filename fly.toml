# fly.toml — TweetSniper worker bot
# No HTTP service — this is a pure background worker process
# Fly.io will keep it running continuously without traffic-based auto-stop

app = 'tweetsniper'
primary_region = 'lhr'

[build]
  dockerfile = 'Dockerfile'

[env]
  PYTHONUNBUFFERED = '1'

[[vm]]
  memory = '256mb'
  cpu_kind = 'shared'
  cpus = 1
